## Context

The reference conversion is split across recipe stages that currently disagree.
`ReplacePersistentWithManyToOneAnnotation` emits `FetchType.EAGER` when `@Persistent(defaultFetchGroup = "true")` is present and otherwise emits `FetchType.LAZY` for ordinary references.
It currently omits `fetch` only for an inferred owning `@OneToOne` with no `@Persistent` metadata, allowing JPA's eager default as an EclipseLink deletion workaround.

The later `com.ecpnv.openrewrite.jdo2jpa.v2x.optional` recipe contains `RemoveAnnotationAttributeConditionally`, which has no datastore-identity or `EntityAbstract` constraint and removes `fetch = FetchType.LAZY` from every matching `@ManyToOne` within the entity compilation units accepted by the recipe.
That broad stage was intended to change JDO's lazy default to JPA's eager default, so it deliberately undoes the earlier semantic translation.
Consumers that compose the base and optional recipes therefore receive different output from tests that exercise `v2x.Persistent` alone.

Recent work for finding 2 strengthened `ReplacePersistentWithManyToOneAnnotation` and added isolated coverage for bare references and bidirectional one-to-one detection.
Finding 3 must preserve that lifecycle work while completing the fetch contract across the composed pipeline.

Estatio now has the finding-3 frozen baseline for the reusable `every_to_one_association_must_declare_fetch` gate introduced by finding 8.
Its reviewed baseline contains 453 generated `@ManyToOne` or `@OneToOne` members without explicit fetch metadata.
That downstream gate supplies structural metadata evidence only; runtime lazy loading, graph traversal, query counts, and EclipseLink integration evidence belong to Estatio rather than this repository.

## Goals / Non-Goals

**Goals:**

- Preserve JDO default-fetch-group intent as explicit JPA metadata for every generated to-one mapping.
- Make isolated, base-composite, and optional-composite recipe paths agree.
- Keep explicit JDO eager opt-in eager and ordinary JDO references lazy.
- Express the inferred owning one-to-one EclipseLink exception as explicit eager metadata.
- Prevent a later recipe stage from silently erasing the chosen fetch strategy.
- Prove the emitted mapping through focused recipe fixtures and a detached Estatio regeneration.

**Non-Goals:**

- Guarantee that a JPA provider honours `FetchType.LAZY` at runtime.
- Translate named JDO fetch groups into a single global JPA mapping.
- Add entity graphs, fetch joins, repository query changes, or `OrmUtil.isJdo()` branches.
- Add or modify Estatio architecture rules, frozen baselines, or query-count tests in this repository.
- Revisit cascade or orphan-removal decisions implemented by finding 2.
- Change collection fetch policy except where an existing one-to-one path is generated by the collection-cardinality recipe.

## Decisions

### Use one explicit fetch truth table

The generated to-one fetch strategy will follow this order:

[cols="2,2,2",options="header"]
|===
|Source relationship metadata
|Generated relationship shape
|Generated fetch

|`defaultFetchGroup = "true"`
|`@ManyToOne` or inferred owning `@OneToOne`
|`FetchType.EAGER`

|`defaultFetchGroup = "false"`
|`@ManyToOne` or inferred owning `@OneToOne`
|`FetchType.LAZY`

|`@Persistent` with omitted `defaultFetchGroup`
|`@ManyToOne` or inferred owning `@OneToOne`
|`FetchType.LAZY`

|No `@Persistent` metadata
|Ordinary `@ManyToOne`
|`FetchType.LAZY`

|No `@Persistent` metadata
|Inferred owning `@OneToOne`
|`FetchType.EAGER`
|===

Explicit `EAGER` on the last row preserves the current provider workaround without relying on JPA's default or violating the downstream explicit-fetch gate.
The presence of `@Persistent` without `defaultFetchGroup` does not constitute eager opt-in because JDO's ordinary reference default remains lazy.

### Always emit the resolved strategy in the relationship recipe

`ReplacePersistentWithManyToOneAnnotation` will resolve one strategy and always append `fetch = FetchType.<strategy>`.
The current `addFetchType` omission branch will be replaced by explicit strategy selection.
This keeps annotation assembly, import management, cascade ordering, join-column generation, and bidirectional cardinality detection in one place.

An alternative is to add a later normalization recipe that fills missing fetch attributes.
That would obscure the JDO metadata used to make the choice and recreate the stage-order problem, so it is rejected.

### Remove the optional lazy-stripping stage

The broad `RemoveAnnotationAttributeConditionally` entry that removes lazy fetch from every matching `@ManyToOne` in accepted entity compilation units will be deleted from `v2x.optional`.
It will not be replaced by an inheritance or identity predicate because neither JPA datastore identity nor `EntityAbstract` inheritance changes JDO's default-fetch-group semantics.
Other optional recipe entries remain unchanged.

This changes generated behaviour for consumers that relied on the optional stage's implicit eager result.
The release notes will call out that intentional compatibility break.

### Test both the decision and recipe composition

Isolated tests will cover all rows of the truth table and assert the relationship kind, fetch value, imports, cascade, and join-column interaction.
Existing finding-2 fixtures will remain as regression coverage for dependency cascades and multiple inverse relationships to the same target type.

`v2x.Persistent` tests will prove that the base composite preserves explicit fetch metadata after JDO annotation cleanup.
`v2x.optional` tests will prove that already generated lazy and eager mappings remain unchanged.
A composed regression test will run the relevant stages in consumer order so a future cleanup recipe cannot reintroduce metadata erasure.
Idempotency coverage will prove that rerunning the applicable pipeline does not remove or duplicate fetch metadata.

### Keep named fetch plans at query level

Membership in a named JDO fetch group does not make a relationship part of the default fetch group.
The static mapping will therefore use only `defaultFetchGroup` when choosing global JPA fetch metadata.
Consumers that activate named JDO plans must preserve those use cases through JPA entity graphs, fetch joins, or repository behaviour in their own codebase.

### Separate recipe A/B validation from consumer-baseline reconciliation

The implementation will build distinct pre-change and candidate jdo2jpa artifacts and run each against the same pinned Estatio `prod` input.
The A/B generated-output delta isolates this change and must contain only intended fetch metadata and directly consequent import or formatting changes.

A separate comparison will reconcile candidate output with the matched Estatio JPA rewrite baseline.
That comparison will explicitly classify output from the already approved stream-order and orphan-removal changes instead of misidentifying those changes as finding-3 regressions.
The `prod` input commit, JPA rewrite-baseline commit, and commit containing the 453-violation finding-3 frozen baseline will be recorded as one matched validation set.

Generated-source inventory and the finding-3 architecture gate will identify to-one mappings that remain structurally implicit.
They will not be presented as runtime lazy-loading, query-count, cycle, or graph-loading evidence.
Frozen-baseline validation will use deliberate baseline-artifact removal with normal creation, update, and refreeze settings still disabled.

The jdo2jpa change will not edit or commit Estatio files.
After release, Estatio must update its recipe version, regenerate the JPA branch, run provider-level tests, and deliberately remove the finding-3 frozen baseline.

## Risks / Trade-offs

- [Risk] Explicit lazy mappings can expose detached-access failures that implicit eager loading previously masked.
  → Mitigation: document the breaking change and hand representative graph-loading and query-count validation to the consuming application.
- [Risk] JPA treats lazy to-one loading as a hint and EclipseLink behaviour depends on static weaving.
  → Mitigation: define this repository's contract as emitted metadata and require provider-level evidence downstream.
- [Risk] Making inferred owning one-to-one mappings explicitly eager can increase graph loading.
  → Mitigation: confine eager fallback to the already documented no-`@Persistent` provider exception and test that explicit JDO choices still win.
- [Risk] Removing the broad optional YAML stage can change a large generated diff.
  → Mitigation: use same-input pre-change/candidate A/B regeneration to isolate finding-3 output, then reconcile the candidate separately with the matched Estatio baseline and previously approved recipe changes.
- [Risk] Annotation argument ordering or import cleanup can make golden tests brittle.
  → Mitigation: assert complete representative outputs while keeping semantic cases small and isolated.
- [Risk] Finding-2 relationship lifecycle changes could regress during fetch refactoring.
  → Mitigation: retain and rerun its dependent-reference and bidirectional-cardinality fixtures.

## Migration Plan

First characterize current isolated, persistent-composite, and optional-composite outputs with focused tests.
Then replace the inferred one-to-one omission with explicit strategy resolution and remove the optional lazy-stripping stage.
Run the focused and complete jdo2jpa test suites, build and install the snapshot, and validate a detached Estatio regeneration.
Publish a release identifying the generated-source breaking change and the downstream validation handoff.

Rollback consists of restoring the optional lazy-stripping stage and the inferred one-to-one omission branch.
Consumers can also pin the previous jdo2jpa release while addressing newly exposed lazy-access assumptions.

## Open Questions

None for recipe implementation.
The selection of stable EclipseLink query-count scenarios remains an Estatio-owned follow-up and does not block this release.
